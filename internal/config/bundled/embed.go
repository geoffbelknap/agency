package bundled

// All hub-managed files (routing, services, ontology) are now synced from
// the hub via `agency hub update`. No embedded defaults in the binary.
// This package is retained for potential future use.
