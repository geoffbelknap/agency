# Live Risky Suite

Put opt-in live tests here when the flow can:

- trigger outbound effects
- alter shared runtime configuration
- install or remove shared components
- create noisy shared data that is harder to clean up deterministically

These tests should never run as part of the default live suite.
